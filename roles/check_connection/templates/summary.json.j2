[
{% for host in ansible_play_hosts %}
{% set inventory=host %}
{% set address=hostvars[host]._addr|d('') %}
{% set port=hostvars[host]._port|d('') %}
{% set result=hostvars[host]._result|d('') %}
  {
    "ansible_host": "{{ host }}",
    "address": "{{ address }}",
    "port": "{{ port }}",
    "result": "{{ result }}"
  }{% if not loop.last %},{% endif %}

{% endfor %}
]