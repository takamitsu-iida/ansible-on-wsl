---

- name: show ip route
  hosts: ce1  # ios_routers
  gather_facts: false
  strategy: linear
  serial: 0

  tasks:
    - include_role:
        name: ../../show_ip_route
      vars:
        SAVE_RESULT: false